# Browser Model Slot

Place the trained YOLO-seg ONNX model here:

```text
assets/models/racket_ball_yoloseg.onnx
```

Until this file exists and a browser ONNX runtime is wired in, the app uses pose-guided fallback estimates for racket and ball overlays.

