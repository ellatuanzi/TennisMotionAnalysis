# Published Tennis Training Diary

This directory is the static, password-protected diary release. Update it only after confirming the local diary.

Build command from the repository root:

```bash
python3 scripts/build_published_diary.py
```
